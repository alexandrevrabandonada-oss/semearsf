import { PagePlaceholder } from "./PagePlaceholder";

export function TransparenciaPage() {
  return (
    <PagePlaceholder
      title="Transparencia"
      description="[Placeholder] Prestacao de contas, documentos e relatatorios. Nenhum dado foi preenchido automaticamente."
    />
  );
}
